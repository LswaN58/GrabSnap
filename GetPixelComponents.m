function compAssignments = GetPixelComponents(img, GMM)
    warning('STUB! GetPixelComponents only returns zeros.')
    [height, width, depth] = size(img);
    compAssignments = zeros(height, width);
end

