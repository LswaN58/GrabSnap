function graph = GenerateGraph(GMMData, compAssignments_fg, compAssignments_bg,...
        weights_fg, means_fg, covars_fg, weights_bg, means_bg, covars_bg)
    warning('STUB! GenerateGraph only returns zeros.')
    [height, width, depth] = size(GMMData);
    graph = zeros(height, width);
end

